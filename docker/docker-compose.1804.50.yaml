version: "3"

services:

  runtime-setup:
    image: swift-service-bootstrap:18.04-5.0
    build:
      args:
        ubuntu_version: "bionic"
        swift_version: "5.0"

  test:
    image: swift-service-bootstrap:18.04-5.0

  shell:
    image: swift-service-bootstrap:18.04-5.0
